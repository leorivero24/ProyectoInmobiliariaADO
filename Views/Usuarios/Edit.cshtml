@model ProyectoInmobiliariaADO.Models.Usuario

@{
    ViewData["Title"] = "Editar Usuario";
    bool esAdmin = User.IsInRole("Administrador");
}

@await Html.PartialAsync("_Navbar")

<div class="container mt-4">
    <h1 class="mb-4 text-warning">
        <i class="bi bi-pencil-square"></i> Editar Usuario
    </h1>

    <div class="card shadow-lg border-0">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0">Formulario de Edición</h4>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post" enctype="multipart/form-data" novalidate>
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />

                <!-- Nombre -->
                <div class="mb-3">
                    <label asp-for="Nombre" class="form-label fw-bold"></label>
                    @* Deshabilitamos la validación automática *@
                    <input asp-for="Nombre" class="form-control" placeholder="Ingrese el nombre" data-val="false" />
                </div>

                <!-- Email -->
                <div class="mb-3">
                    <label asp-for="Email" class="form-label fw-bold"></label>
                    <input asp-for="Email" class="form-control" placeholder="ejemplo@email.com" data-val="false" />
                </div>

                <!-- Nueva contraseña -->
                <div class="mb-3">
                    <label asp-for="PasswordHash" class="form-label fw-bold">Nueva Contraseña</label>
                    <input asp-for="PasswordHash" class="form-control" type="password" placeholder="Ingrese nueva contraseña" data-val="false" />
                </div>

                <!-- Rol (solo admin) -->
                @if (esAdmin)
                {
                    <div class="mb-3">
                        <label asp-for="Rol" class="form-label fw-bold">Rol</label>
                        <select asp-for="Rol" class="form-select" asp-items="ViewBag.Roles" data-val="false"></select>
                    </div>
                }

                <!-- Avatar -->
                <div class="mb-3">
                    <label class="form-label fw-bold">Avatar</label>
                    <input type="file" name="AvatarFile" class="form-control" data-val="false" />
                    @if (!string.IsNullOrEmpty(Model.Avatar))
                    {
                        <div class="mt-2">
                            <img src="~/uploads/@Model.Avatar" alt="Avatar" width="100" class="img-thumbnail" />
                            <div class="form-check mt-1">
                                <input class="form-check-input" type="checkbox" name="RemoveAvatar" value="true" id="removeAvatar">
                                <label class="form-check-label" for="removeAvatar">Eliminar avatar</label>
                            </div>
                        </div>
                    }
                </div>

                <!-- Botones -->
                <div class="d-flex gap-2 mt-4">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-save"></i> Guardar
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts{
    @* Evitar que jQuery Validation bloquee campos vacíos *@
    <script>
        $(function () {
            $("form").removeData("validator").removeData("unobtrusiveValidation");
        });
    </script>
}
